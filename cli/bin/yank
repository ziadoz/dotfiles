#!/usr/bin/env bash
set -euo pipefail

# @see: https://evanhahn.com/scripts-i-wrote-that-i-use-all-the-time/

if [ "${1:-}" = "-h" ] || [ "${1:-}" = "--help" ]; then
    echo "Yank"
    echo "Output clipboard contents to stdout"
    echo "Usage: yank"
    exit 0
fi

pbpaste
